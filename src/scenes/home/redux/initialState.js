const initialState = {
  isFetching: false,
  isDone: false,
  errorMessage: '',
  cachePictures: [],
  mode: 'online',
  isConnected: true,
  percent: 0,
  isSync: false,
};

export default initialState;
